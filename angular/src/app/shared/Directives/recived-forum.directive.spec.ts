import { RecivedForumDirective } from './recived-forum.directive';

describe('RecivedForumDirective', () => {
  it('should create an instance', () => {
    const directive = new RecivedForumDirective();
    expect(directive).toBeTruthy();
  });
});
