import { RecivedDirective } from './recived.directive';

describe('RecivedDirective', () => {
  it('should create an instance', () => {
    const directive = new RecivedDirective();
    expect(directive).toBeTruthy();
  });
});
